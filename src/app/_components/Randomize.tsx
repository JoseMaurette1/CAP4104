import React from "react";

const Randomize = () => {
  return (
    <>
      {/* Take pics that user uploads and develop sorting algorithm to randomize them in different positions and sizes */}
    </>
  );
};

export default Randomize;
